@{
    ViewBag.Title = "UserDashBoard";
}

<fieldset>
    <legend > User DashBoard </legend>

@if (Session["usuarioNombre"] != null){<text>
    Welcome @Session["usuarioNombre"].ToString() </text>
    } </fieldset>  
